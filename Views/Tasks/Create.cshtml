@model LearnMvc.Models.TaskItem

@{
    ViewData["Title"] = "Create New Task";
}

<div class="container mt-4">
    <h1>Create New Task</h1>
    
    <div class="row">
        <div class="col-md-6">
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                
                <div class="form-group mb-3">
                    <label asp-for="Name" class="form-label">Task Name</label>
                    <input asp-for="Name" class="form-control" placeholder="Enter task name" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
                
                <div class="form-check mb-3">
                    <input class="form-check-input" asp-for="IsComplete" />
                    <label class="form-check-label" asp-for="IsComplete">
                        Mark as complete
                    </label>
                </div>
                
                <div class="form-group">
                    <button type="submit" class="btn btn-primary">Create</button>
                    <a asp-action="Index" class="btn btn-outline-secondary">Back to List</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
